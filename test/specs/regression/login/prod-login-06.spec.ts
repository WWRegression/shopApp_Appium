import { runOrSkip } from '../../../helpers/tc-filter.helper';

describe('PROD_LOGIN_06', () => {
  it('Guest - auto login via Login CTA on cart', async function () {
    await runOrSkip.call(this, 'PROD_LOGIN_06', async () => {
      // TODO
    });
  });
});
