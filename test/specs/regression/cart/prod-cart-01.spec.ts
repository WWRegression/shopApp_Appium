import { runOrSkip } from '../../../helpers/tc-filter.helper';

describe('PROD_CART_01', () => {
  it('quantity +/-, product removal, cart icon count', async function () {
    await runOrSkip.call(this, 'PROD_CART_01', async () => {
      // TODO
    });
  });
});
