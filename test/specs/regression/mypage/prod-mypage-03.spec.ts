import { runOrSkip } from '../../../helpers/tc-filter.helper';

describe('PROD_MYPAGE_03', () => {
  it('each menu (My Orders~Inbox) redirects to the correct page', async function () {
    await runOrSkip.call(this, 'PROD_MYPAGE_03', async () => {
      // TODO
    });
  });
});
