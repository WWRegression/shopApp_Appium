import { runOrSkip } from '../../../helpers/tc-filter.helper';

describe('PROD_MYPAGE_08', () => {
  it('Search/Chat icons on Account redirect correctly', async function () {
    await runOrSkip.call(this, 'PROD_MYPAGE_08', async () => {
      // TODO
    });
  });
});
