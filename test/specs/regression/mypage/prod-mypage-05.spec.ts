import { runOrSkip } from '../../../helpers/tc-filter.helper';

describe('PROD_MYPAGE_05', () => {
  it('each sub-menu (Support~login) redirects correctly', async function () {
    await runOrSkip.call(this, 'PROD_MYPAGE_05', async () => {
      // TODO
    });
  });
});
