import { runOrSkip } from '../../../helpers/tc-filter.helper';

describe('PROD_MYPAGE_04', () => {
  it('Recommended Product under Registered Devices redirects to BC/PD', async function () {
    await runOrSkip.call(this, 'PROD_MYPAGE_04', async () => {
      // TODO
    });
  });
});
