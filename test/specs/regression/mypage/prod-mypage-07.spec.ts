import { runOrSkip } from '../../../helpers/tc-filter.helper';

describe('PROD_MYPAGE_07', () => {
  it('Estore/EPP option displayed on Account page', async function () {
    await runOrSkip.call(this, 'PROD_MYPAGE_07', async () => {
      // TODO
    });
  });
});
