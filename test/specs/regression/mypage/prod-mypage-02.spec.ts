import { runOrSkip } from '../../../helpers/tc-filter.helper';

describe('PROD_MYPAGE_02', () => {
  it('each Dashboard menu redirects to the correct page', async function () {
    await runOrSkip.call(this, 'PROD_MYPAGE_02', async () => {
      // TODO
    });
  });
});
