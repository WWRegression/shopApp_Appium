import { runOrSkip } from '../../../helpers/tc-filter.helper';

describe('PROD_SEARCH_08', () => {
  it('add/remove wishlist from Search PF or wishlist page', async function () {
    await runOrSkip.call(this, 'PROD_SEARCH_08', async () => {
      // TODO
    });
  });
});
