import { runOrSkip } from '../../../helpers/tc-filter.helper';

describe('PROD_SEARCH_02', () => {
  it('trending search and save/delete search history', async function () {
    await runOrSkip.call(this, 'PROD_SEARCH_02', async () => {
      // TODO
    });
  });
});
